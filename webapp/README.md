# 📱 Telegram WebApp - مدیریت شخصی

## 📋 فهرست
- [معرفی](#معرفی)
- [ویژگی‌ها](#ویژگی‌ها)
- [نصب و راه‌اندازی](#نصب-و-راهاندازی)
- [استفاده](#استفاده)
- [ساختار پروژه](#ساختار-پروژه)

## معرفی
یک وب‌اپ تلگرام مدرن و زیبا با Material Design برای مدیریت درآمدها، یادآورها، یادداشت‌ها و عادت‌های روزانه.

## ویژگی‌ها
✨ **طراحی Material Design** - رابط کاربری مدرن و زیبا
🎨 **حالت تاریک** - پشتیبانی کامل از Dark Mode
📱 **Responsive** - سازگار با تمام دستگاه‌ها
⚡ **سریع و روان** - بدون لگ و تاخیر
🔔 **اعلان‌های هوشمند** - نوتیفیکیشن‌های Material
🎯 **داشبورد جامع** - نمای کلی از همه فعالیت‌ها
💰 **مدیریت درآمد** - ثبت و مشاهده درآمدها
⏰ **یادآورها** - مدیریت یادآورهای روزانه
📝 **یادداشت** - ثبت یادداشت‌های سریع
✅ **عادت‌ها** - پیگیری عادت‌های روزانه

## نصب و راه‌اندازی

### گام 1: Clone کردن پروژه
```bash
# Clone کردن برنچ webapp
git clone -b webapp https://github.com/miladrajabi2002/miladrajabi.git telegram-webapp
cd telegram-webapp
```

### گام 2: آپلود فایل‌ها
فایل‌های موجود در پوشه `webapp` را روی هاست خود آپلود کنید.

### گام 3: ایجاد Bot Menu Button
در BotFather از دستورات زیر استفاده کنید:

```
/mybots
[انتخاب ربات خود]
Bot Settings
Menu Button
Configure menu button
```

سپس:
- **Button text**: داشبورد من 📊
- **Button URL**: https://yourdomain.com/webapp/

### گام 4: تنظیم Web App در ربات
در کد ربات خود (index.php) این کد را اضافه کنید:

```php
// نمایش دکمه WebApp در منوی اصلی
$keyboard = [
    'inline_keyboard' => [
        [['text' => '📊 داشبورد وب', 'web_app' => ['url' => 'https://yourdomain.com/webapp/']]],
        // ... بقیه دکمه‌ها
    ]
];
```

## استفاده

### روش 1: از طریق Menu Button
1. ربات را باز کنید
2. روی دکمه Menu (کنار فیلد پیام) کلیک کنید
3. گزینه "داشبورد من" را انتخاب کنید

### روش 2: از طریق دکمه Inline
1. ربات را باز کنید  
2. دستور /start را بزنید
3. روی دکمه "📊 داشبورد وب" کلیک کنید

### روش 3: لینک مستقیم
می‌توانید مستقیماً از لینک زیر استفاده کنید:
```
https://t.me/YOUR_BOT_USERNAME?start=webapp
```

## ساختار پروژه

```
webapp/
├── index.html          # صفحه اصلی
├── css/
│   └── style.css       # استایل‌های Material Design
├── js/
│   └── app.js          # منطق برنامه
└── README.md           # مستندات
```

## تنظیمات پیشرفته

### اتصال به API
برای اتصال به بک‌اند، فایل `js/app.js` را ویرایش کنید:

```javascript
const API_URL = 'https://yourdomain.com/api/';

// مثال دریافت درآمدها
async function loadIncomes() {
    const response = await fetch(API_URL + 'incomes.php', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
        },
        body: JSON.stringify({
            user_id: tg.initDataUnsafe.user.id
        })
    });
    const data = await response.json();
    // پردازش داده‌ها...
}
```

### سفارشی‌سازی رنگ‌ها
در فایل `css/style.css` متغیرهای CSS را تغییر دهید:

```css
:root {
    --primary-color: #YOUR_COLOR;
    --secondary-color: #YOUR_COLOR;
    /* ... */
}
```

## پشتیبانی
برای گزارش باگ یا درخواست ویژگی جدید، یک Issue ایجاد کنید.

## لایسنس
MIT License

---

**ساخته شده با ❤️ برای جامعه توسعه‌دهندگان ایرانی**